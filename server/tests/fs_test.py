import os
from arlo_st import libraryFs

#def test_get_all(fs_root):
#    actual_entries = os.listdir(fs_root)
#    libraryFs_entries = libraryFs.getAll()
#    assert len(libraryFs) == len(actual_entries)
